apiVersion: v1
kind: Service

metadata:
  name: circlechat-backend-service

spec:
  selector:
    app: circlechat-backend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP